const labels = document.querySelectorAll('.form-control label')
console.log(labels)
// labels.forEach((label) => {
//     label.addEventListener('click', () => {
//         label.classList.add('top')
//         console.log(label)
//     })
//     label.classList.remove('top')
// })

// labels.forEach((label) => {
//     label.innerHTML = label.innerText.split('').map((letter) =>
//         console.log(letter),
//         `<span style=" transition-delay:${index * 50}ms">${letter}
//     </span>`).join('')
// })
